# Analog-Pin-Voltage-Reader
An Arduino project that reads the analog voltage from a potentiometer connected to pin A3 and displays the measured voltage in real time on a 16×2 LCD using the Adafruit LiquidCrystal library. It includes a startup splash screen and updates the voltage display every 100 ms.
